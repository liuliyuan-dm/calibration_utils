step1: 内参标定的专用高精度棋盘格（格子数目12*9，角点数据11*8，网格大小45mm）
step2: 标定时固定相机，适当倾斜、旋转棋盘格（不要翻转），变换不同姿态，注意保持棋盘格在画面中占据1/3到1/2左右的画面。（此处也可以固定棋盘格，旋转相机，或者两个都旋转也可以）
step3: 通过web前端界面可以直接download保存图片（默认1920*1080），一共需要采集20-25张左右（最终保证能够提取到角点的有效图片在15-25张即可
step4: 图片要求：希望采集的20多张图片中，棋盘格能够分别覆盖画面中间，上下左右边，四个对角，利于标定畸变。
step5: 建议把采集到的图片放到data/cali/路径下对应的文件夹中,执行本程序，输出内参文件camera_**.intrinsics.proto.txt,**代表相机名称。 



程序编译步骤：
cd build
cmake ..
make

程序执行步骤：
./src/camera_calibration_main input_file_path output_file_path
